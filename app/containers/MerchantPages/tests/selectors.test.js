import expect from 'expect';
// import { selectMerchantPagesDomain } from '../selectors';

// const selector = makeSelectMerchantPagesDomain();

describe('makeSelectMerchantPagesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
