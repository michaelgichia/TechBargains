import expect from 'expect';
// import { selectMerchantPageDomain } from '../selectors';

// const selector = makeSelectMerchantPageDomain();

describe('makeSelectMerchantPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
