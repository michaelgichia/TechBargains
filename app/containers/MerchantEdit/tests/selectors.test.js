import expect from 'expect';
// import { selectMerchantEditDomain } from '../selectors';

// const selector = makeSelectMerchantEditDomain();

describe('makeSelectMerchantEditDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
