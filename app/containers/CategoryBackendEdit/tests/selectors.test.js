import expect from 'expect';
// import { selectCategoryBackendEditDomain } from '../selectors';

// const selector = makeSelectCategoryBackendEditDomain();

describe('makeSelectCategoryBackendEditDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
