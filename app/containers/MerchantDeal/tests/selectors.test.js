import expect from 'expect';
// import { selectMerchantDealDomain } from '../selectors';

// const selector = makeSelectMerchantDealDomain();

describe('makeSelectMerchantDealDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
