import expect from 'expect';
// import { selectEditItemDomain } from '../selectors';

// const selector = makeSelectEditItemDomain();

describe('makeSelectEditItemDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
