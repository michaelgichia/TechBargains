import expect from 'expect';
// import { selectFlashMessageDomain } from '../selectors';

// const selector = makeSelectFlashMessageDomain();

describe('makeSelectFlashMessageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
