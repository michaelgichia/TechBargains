import expect from 'expect';
// import { selectProductModalDomain } from '../selectors';

// const selector = makeSelectProductModalDomain();

describe('makeSelectProductModalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
