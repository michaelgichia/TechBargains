import expect from 'expect';
// import { selectCategoryFrontPageDomain } from '../selectors';

// const selector = makeSelectCategoryFrontPageDomain();

describe('makeSelectCategoryFrontPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
