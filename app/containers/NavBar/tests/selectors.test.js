import expect from 'expect';
// import { selectNavBarDomain } from '../selectors';

// const selector = makeSelectNavBarDomain();

describe('makeSelectNavBarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
