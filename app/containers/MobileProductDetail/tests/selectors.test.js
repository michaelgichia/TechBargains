import expect from 'expect';
// import { selectMobilePoductDetailDomain } from '../selectors';

// const selector = makeSelectMobilePoductDetailDomain();

describe('makeSelectMobilePoductDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
